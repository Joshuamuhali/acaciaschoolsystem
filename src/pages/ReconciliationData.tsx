import React from 'react';
import SchoolReconciliationReader from '@/components/SchoolReconciliationReader';

const ReconciliationData: React.FC = () => {
  return (
    <div className="min-h-screen bg-gray-50">
      <SchoolReconciliationReader />
    </div>
  );
};

export default ReconciliationData;
